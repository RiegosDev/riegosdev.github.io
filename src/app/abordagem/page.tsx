import Abordagem from '@/components/Abordagem';

export const metadata = {
  title:
    'Nossa Abordagem | Daniel & Tiago',
  description:
    'Conheça os arquitetos por trás das soluções da Riegos.dev.',
};

export default function AbordagemPage() {
  return <Abordagem />;
}
