version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: riegos_app
    restart: always
    ports:
      - '3001:3000' # <-- Expõe a 3001 na VPS e joga pra 3000 do contêiner
    environment:
      - NODE_ENV=production
